type File {
  filename: String!
  mimetype: String!
  encoding: String!
}

type Analyze {
  name: String!
}

type Query {
  upload: [File]
}

type Mutation {
  upload(file: Upload!): Boolean!
  analyze: Analyze
}