type Count {
  Aho: Int!
  Brad: Int!
  Brett: Int!
}

type Analyze {
  perDay: Int!
  # right now perUser comes back as an object - change this to array
  perUser: Count!
}

type Query {
  analyze: Analyze!
}